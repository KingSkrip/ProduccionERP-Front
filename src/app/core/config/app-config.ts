import { AppConfig } from './app-config.model';

export const APP_CONFIG: AppConfig = {
  apiUrl: 'http://localhost:8000/api/',  // URL base de tu API local
  appName: 'Mi Aplicación Angular',
  environment: 'dev',
  featureFlagX: true,
};
