import { AppConfig } from './app-config.model';

export const APP_CONFIG: AppConfig = {
  apiUrl: 'https://trend-being-contests-ashley.trycloudflare.com/api/',
  apiBase:'https://trend-being-contests-ashley.trycloudflare.com',
  appName: 'Fibrasan',
  environment: 'dev',
  featureFlagX: true,
};
